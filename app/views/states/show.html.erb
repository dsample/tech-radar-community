<div class="button-bar">
	<div class="button-group">
		<%= link_to 'Back', states_path, class: 'small secondary button' %>
	</div>
	<div class="button-group">
		<%= link_to 'Edit', edit_state_path(@state), class: 'small button' %>
	</div>
</div>

<h2><%= @state.name %></h2>

<div><%= md @state.description %></div>