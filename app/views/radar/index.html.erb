<h1>Tech Radar</h1>

<% @categories.each do |category| %>
	<h2 class="subheader"><%=category.name.pluralize%></h2>

	<ul class="no-bullet">
	<% category.technologies.each do |tech| %>
		<li><%=link_to tech.name, technology_path(tech, company: current_tenant) %></li>
	<% end %>
	</ul>
<% end %>