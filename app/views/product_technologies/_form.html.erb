<%= form_for(@product_technology, :html => {:class => "custom"}) do |f| %>
  <% if @product_technology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_technology.errors.count, "error") %> prohibited this product_technology from being saved:</h2>

      <ul>
      <% @product_technology.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
